import { ItemByCategoryPipe } from './item-by-category.pipe';

describe('ItemByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
